# shared/

Protocol definitions shared by client and server. Not started.

Whatever lives here must be the single source of truth for message shapes --
defined once, consumed by both sides, never duplicated.
